# Default / Vercel. Keep this tiny — Vercel functions cap at 500 MB unzipped.
# Full local pipelines (Council, QuanTum, PDF): pip install -r requirements-local.txt
fastapi>=0.110.0
pydantic>=2.0
requests>=2.32
feedparser>=6.0
pyyaml>=6.0
uvicorn>=0.28.0
